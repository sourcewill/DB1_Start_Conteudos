------------------------------------------------

Aula: Versionamento (GIT)

Data: 07 e 08/11/2019

Professores: Wilde, ...

------------------------------------------------


$ git config --global user.name "William Rodrigues"
$ git config --global user.email will@uem.br


$ git init 
Inicia um repositório no diretório atual.

$ git status
Exibe o estado atual.

$ git add arquivo1 arquivo 2 ...
Adiciona arquivos ao repositório local (use . para todos)

$ git reset arquivo1 arquivo2 ...
Remove arquivos do repositório local (use . para todos)

$ git commit -m "Mensagem"

$ git commit --amend --reset-author
Modifica o autor do ultimo commit realizado


$ git log
Informa os commits realizados

$ git remote add Nome Url
Configura o repositorio remoto (por padrao usa-se o nome origin)

$ git push origin master
Envia os commits e alteracoes para beanch especificada (neste caso, master)

$ git branch
Exibe todas as branchs

$ git branch nova_branch
Cria uma nova branch

$ git checkout branch
Muda para uma branch existente

$ git merge master
Faz merge da master para branch atual

$ git -d nome_branch
Apaga a branch no repositorio local

$ git -D nome_branch
Apaga uma branch no repositorio remoto

$ git pull
Atualiza o repoitorio local com os arquivos contidos na branch atual



